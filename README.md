# WhatsApp Assistant (Wassist)
	An android assistant that manages routine WhatsApp reminders to select
	contacts.
 
## Usage
	To setup your env, and build the app, navigate to the Project root.
	Then run:
	bash```./scripts/build.sh```

